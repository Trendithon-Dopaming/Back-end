spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/dopaming
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: false

jwt:
  secret: "trendidhondopamingsecretkey"